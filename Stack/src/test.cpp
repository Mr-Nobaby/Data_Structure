#include<stdio.h>
#include<stdlib.h>
#include <iostream>
#include "stack.h"
using namespace std;

int main()
{
	//Node* top = NULL;

	//Push(top, 1);
	//Push(top, 2);
	//Push(top, 3);
	//Print(top);

	//Pop(top);
	//Print(top);

}

